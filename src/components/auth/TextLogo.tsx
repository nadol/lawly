/**
 * Application logo displayed as styled text.
 * Uses brand typography (32px, blue color).
 */
export function TextLogo() {
  return (
    <h1 className="text-3xl font-bold text-blue-600">
      Lawly
    </h1>
  );
}
